[package]
name = "arcdps"
version = "0.24.0"
authors = ["Zerthox", "Greaka"]
edition = "2024"
description = "Rust bindings for ArcDPS plugins"
repository = "https://github.com/zerthox/arcdps-rs"
rust-version = "1.88.0"
categories = ["api-bindings"]
keywords = ["arcdps", "guild-wars-2", "ffi", "addon", "framework"]
license = "MIT"

[dependencies]
arcdps_codegen = { path = "../arcdps_codegen", optional = true }
# Fork of imgui-rs `main` vendoring Dear ImGui 1.92.7 with arcdps's stock imconfig.
# arcdps has linked 1.92.7 since its 20260507 build and ignores the UI callbacks of any
# addon reporting a different IMGUI_VERSION_NUM; arcdps-imgui 0.8 is Dear ImGui 1.80.
imgui = { path = "../../imgui-rs/imgui", features = ["tables-api"] }
bitflags = { workspace = true }
evtc = { path = "../evtc", features = ["realtime"] }
log = { version = "0.4.17", features = ["std"], optional = true }
num_enum = { workspace = true }
serde = { workspace = true, features = ["derive"], optional = true }
strum = { workspace = true, features = ["derive"], optional = true }
unofficial_extras = { path = "../unofficial_extras", optional = true }
windows = { workspace = true, features = [
    "System",
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_System_LibraryLoader",
    "Win32_System_ProcessStatus",
    "Win32_System_Threading",
    "Win32_Graphics_Dxgi",
    "Win32_Graphics_Direct3D11",
] }

[features]
default = ["codegen", "panic", "panic_trace", "panic_msgbox", "unwind"]
codegen = ["dep:arcdps_codegen"]
extras = ["dep:unofficial_extras", "arcdps_codegen/extras"]
log = ["dep:log"]
panic = []
panic_trace = ["panic"]
panic_msgbox = ["panic"]
serde = ["dep:serde", "evtc/serde", "unofficial_extras/serde"]
strum = ["dep:strum", "evtc/strum", "unofficial_extras/strum"]
unofficial_extras = ["extras"]
unwind = ["unofficial_extras/unwind", "arcdps_codegen/unwind"]
